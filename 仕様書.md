# Trail Condition Portal 仕様書

## プロジェクト概要

登山道の危険情報を複数のプラットフォーム（YAMAP、ヤマレコ等）から収集し、統合表示するWebアプリケーション。

### 目的
- 登山者に危険情報を一元的に提供
- 実用性のある公開サービスとして運用

---

## 技術スタック

### バックエンド
- Django 5.x
- PostgreSQL
- Docker + Docker Compose
- uv (パッケージ管理)

### スクレイピング・AI
- httpx (非同期HTTP通信)
- BeautifulSoup4
- DeepSeek API (LLM正規化)
- Pydantic (スキーマ定義)

### 定期実行
- 1日1回の定時実行
- 候補: APScheduler, Celery, cron

### Django設定
```python
# settings.py
LANGUAGE_CODE = "ja"
TIME_ZONE = "Asia/Tokyo"
USE_I18N = True
USE_TZ = True  # データベースはUTC保存、表示はJST
```

---

## データベースモデル

### 1. AreaName (エリア定義)
```python
class AreaName(models.TextChoices):
    OKUTAMA = "OKUTAMA", "奥多摩"
    OKUTAMA_WEST = "OKUTAMA_WEST", "奥多摩西部"
    OKUTAMA_LAKE = "OKUTAMA_LAKE", "奥多摩湖"
    KOSHU = "KOSHU", "甲州"
    TANZAWA = "TANZAWA", "丹沢"
```

**特徴:**
- TextChoicesで定義
- Pydanticでも使用可能（Django TextChoices = str + Enum）

---

### 2. StatusType (状況種別)
```python
class StatusType(models.TextChoices):
    CLOSURE = "CLOSURE", "🚧 通行止め・閉鎖"
    HAZARD = "HAZARD", "⚠️ 危険箇所・通行注意"
    SNOW = "SNOW", "❄️ 積雪・アイスバーン"
    ANIMAL = "ANIMAL", "🐻 動物出没"
    WEATHER = "WEATHER", "🌧️ 気象警報"
    FACILITY = "FACILITY", "🏠 施設情報"
    WATER = "WATER", "💧 水場状況"
    OTHER = "OTHER", "📝 その他"
```

**特徴:**
- モデル外で定義（再利用のため）
- Pydanticでもそのまま使用可能
- JSON Schemaに自動変換（AIに渡す）

---

### 3. DataSource (情報源)
```python
class DataSource(models.Model):
    name = models.CharField("情報源名", max_length=100, unique=True)
    base_url = models.URLField("ベースURL")
    scraping_url_1 = models.URLField("スクレイピング対象URL1", blank=True)
    scraping_url_2 = models.URLField("スクレイピング対象URL2", blank=True)
    is_active = models.BooleanField("有効", default=True)
    created_at = models.DateTimeField(auto_now_add=True)
    updated_at = models.DateTimeField(auto_now=True)
```

**用途:**
- スクレイピング対象サイトの管理
- 例: YAMAP、ヤマレコ、奥多摩ビジターセンター

---

### 4. MountainGroup (山グループ) ※将来実装
```python
class MountainGroup(models.Model):
    name = models.CharField("山グループ名", max_length=100, unique=True)
    area = models.CharField("エリア", max_length=20, choices=AreaName.choices)
    description = models.TextField("説明", blank=True)
    created_at = models.DateTimeField(auto_now_add=True)
    updated_at = models.DateTimeField(auto_now=True)
```

**用途:**
- 地図UI実装時に使用
- 現時点では実装不要

---

### 5. MountainAlias (山名別名) ※将来実装
```python
class MountainAlias(models.Model):
    mountain_group = models.ForeignKey(
        MountainGroup,
        on_delete=models.CASCADE
    )
    alias_name = models.CharField("別名", max_length=100)
    
    class Meta:
        unique_together = [["mountain_group", "alias_name"]]
```

**用途:**
- 山名の表記ゆれを吸収
- 例: "馬頭刈山" → "大岳山グループ"

**Signal連携:**
- MountainAlias追加時、過去のTrailConditionも自動更新

---

### 6. TrailCondition (登山道状況) ※コアモデル
```python
class TrailCondition(models.Model):
    # 情報源
    source = models.ForeignKey(
        DataSource,
        on_delete=models.CASCADE,
        verbose_name="情報源"
    )
    url = models.URLField("元記事URL", max_length=500)
    
    # 原文（スクレイピング結果そのまま）
    mountain_name_raw = models.CharField("山名（原文）", max_length=50)
    trail_name = models.CharField("登山道名（原文）", max_length=50)
    title = models.CharField("タイトル（原文）", max_length=200)
    description = models.TextField("詳細説明（原文）")
    reported_at = models.DateTimeField("報告日時")
    
    # 半正規化（スクレイピング時に取得）
    area = models.CharField(
        "エリア",
        max_length=20,
        choices=AreaName.choices,
        help_text="情報源から取得したエリア情報"
    )
    
    # 正規化済み（AI判定）
    status = models.CharField(
        "状況種別",
        max_length=20,
        choices=StatusType.choices
    )
    
    # 将来実装
    mountain_group = models.ForeignKey(
        MountainGroup,
        on_delete=models.SET_NULL,
        null=True,
        blank=True,
        verbose_name="山グループ"
    )
    
    # メタ情報
    is_active = models.BooleanField(
        "有効",
        default=True,
        help_text="誤情報や古い情報を無効化。通行止め解除は新レコードで表現"
    )
    created_at = models.DateTimeField(auto_now_add=True)
    updated_at = models.DateTimeField(auto_now=True)
    
    class Meta:
        indexes = [
            models.Index(fields=["mountain_name_raw", "trail_name", "-reported_at"]),
            models.Index(fields=["area", "status", "-reported_at"]),
        ]
```

---

## データフロー

### 1. スクレイピング
```
DataSource (YAMAP, ヤマレコ等)
    ↓
httpx で非同期取得
    ↓
BeautifulSoup でパース
    ↓
生データ抽出
{
  url: "https://yamap.com/activities/12345",
  title: "雲取山 鴨沢ルートで落石多数",
  content: "本文...",
  reported_at: "2025-01-01T10:00:00+09:00",
  area: "奥多摩"  # 情報源から取得
}
```

**実装:**
- 2段階スクレイピング
  1. 検索結果ページから記事URLリスト取得
  2. 各記事の詳細情報取得
- robots.txt 遵守
- data-testid 等の安定したセレクタ使用

---

### 2. AI正規化
```
生データ
    ↓
Pydantic スキーマ定義
    ↓
JSON Schema 生成
    ↓
DeepSeek API に送信
    ↓
構造化された出力
{
  mountain_name_raw: "雲取山",
  trail_name: "鴨沢ルート",
  status: "HAZARD",
  description: "落石多数"
}
```

**Pydantic スキーマ例:**
```python
from pydantic import BaseModel, Field
from trail_status.models.condition import StatusType

class TrailConditionAISchema(BaseModel):
    mountain_name_raw: str = Field(description="山名（原文そのまま）")
    trail_name: str = Field(description="登山道名")
    status: StatusType = Field(description="状況種別")
    description: str = Field(description="詳細説明")
```

**JSON Schema 自動生成:**
```python
schema = TrailConditionAISchema.model_json_schema()
# StatusType の enum が自動的に含まれる
```

---

### 3. データベース保存
```
AI正規化結果 + スクレイピング元情報
    ↓
TrailCondition.objects.create(**data)
    ↓
データベース（PostgreSQL）
```

**実装:**
```python
TrailCondition.objects.create(
    source=data_source,
    url=scraped_data['url'],
    mountain_name_raw=ai_output.mountain_name_raw,
    trail_name=ai_output.trail_name,
    area=scraped_data['area'],
    status=ai_output.status,
    description=ai_output.description,
    reported_at=scraped_data['reported_at']
)
```

---

## 非同期処理

### httpx 使用

**同期モード（Phase 1）:**
```python
import httpx

with httpx.Client() as client:
    response = client.get(url)
```

**非同期モード（Phase 2）:**
```python
import asyncio
import httpx

async def scrape_all():
    async with httpx.AsyncClient() as client:
        responses = await asyncio.gather(
            client.get(url1),
            client.get(url2),
            client.get(url3)
        )
    return responses

# Django管理コマンドから実行
def handle():
    results = asyncio.run(scrape_all())
```

**理由:**
- DeepSeek はレート制限なし
- 並列処理で大幅高速化（10倍）
- requests と API互換性あり

---

## 定期実行

### Django管理コマンド
```python
# management/commands/scrape.py
from django.core.management.base import BaseCommand
from trail_status.scraper import scrape_all

class Command(BaseCommand):
    help = "スクレイピングを実行"
    
    def handle(self, *args, **options):
        self.stdout.write("開始...")
        scrape_all()
        self.stdout.write(self.style.SUCCESS("完了"))
```

**実行:**
```bash
docker compose exec web uv run manage.py scrape
```

### 定期実行方式（後で決定）

**候補:**
1. APScheduler（推奨）
   - Djangoと一緒に起動
   - 追加インフラ不要
   
2. Celery
   - 本格的
   - Redis等が必要
   
3. cron
   - シンプル
   - サーバー依存

---

## 運用方針

### is_active フィールドの用途

**用途:**
- 誤情報の無効化
- 古い情報の無効化（自動）
- 管理者による手動無効化

**通行止め解除の表現:**
- 新しいレコードを作成（推奨）
- `status=SAFE` で新規作成

**例:**
```python
# 通行止め発生
TrailCondition.objects.create(
    mountain_name_raw="雲取山",
    trail_name="鴨沢ルート",
    status=StatusType.CLOSURE,
    reported_at="2025-01-01"
)

# 解除（新規レコード）
TrailCondition.objects.create(
    mountain_name_raw="雲取山",
    trail_name="鴨沢ルート",
    status=StatusType.SAFE,
    reported_at="2025-01-10"
)

# 最新状態を取得
latest = TrailCondition.objects.filter(
    mountain_name_raw="雲取山",
    trail_name="鴨沢ルート",
    is_active=True
).order_by('-reported_at').first()
```

---

## 開発フェーズ

### Phase 1: 基本実装（現在）
- ✅ モデル設計
- ✅ Docker環境構築
- ✅ PostgreSQL接続
- ⏳ スクレイピング実装
- ⏳ AI正規化実装
- ⏳ 定期実行設定

### Phase 2: 機能拡張
- 非同期処理の最適化
- エラーハンドリング
- ログ管理
- Web API実装

### Phase 3: フロントエンド
- 検索UI
- 地図表示
- フィルタリング

### Phase 4: 高度な機能（将来）
- MountainGroup 実装
- AI候補提案（確信度付き）
- 学習ループ

---

## 技術的な判断

### CharField の null/blank ポリシー
```python
# 推奨
field = models.CharField(max_length=100, blank=True, default="")
# 空の状態を "" に統一（NULL と "" を混在させない）

# 例外
foreign_key = models.ForeignKey(null=True, blank=True)
# ForeignKey は null=True が自然
```

### タイムゾーン管理

- データベース: UTC で保存
- 表示: JST に自動変換
- `USE_TZ = True` 必須
- モデル定義にタイムゾーン指定不要

### Enum の管理

- `models.TextChoices` を使用
- クラス外で定義（再利用のため）
- Pydantic でそのまま使用可能
- マイグレーション不要（choices は DB に保存されない）

---

## まとめ

### コアコンセプト
- スクレイピング → AI正規化 → DB保存
- 非同期処理で高速化
- 1日1回の定期実行
- 実用的な公開サービス

### 技術選定の理由
- Django: 管理画面、ORM、豊富なエコシステム
- PostgreSQL: 信頼性、拡張性
- httpx: 非同期対応、requests互換
- DeepSeek: レート制限なし、コスト効率
- Pydantic: 型安全、JSON Schema自動生成

### 設計原則
- 原文は保存（トレーサビリティ）
- 正規化は別カラム（元データ保護）
- 履歴を残す（新レコード作成）
- 段階的な実装（MountainGroup は後回し）